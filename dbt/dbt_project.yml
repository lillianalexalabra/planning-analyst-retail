# dbt/dbt_project.yml
name: planning_analyst_retail
version: '1.0.0'
config-version: 2

profile: planning_analyst_retail

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  planning_analyst_retail:
    staging:
      +materialized: view
    mart:
      +materialized: table
